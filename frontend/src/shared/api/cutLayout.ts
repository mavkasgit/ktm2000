/**
 * Раскрой позиции: вход и распилы по длинам (ADR-0002/0003).
 * Приходит с бэкенда (`format_cut_layout`) — фронт рисует, не форматирует.
 */
export type CutLayout = {
  /**
   * Габарит входа: «2,75» — либо одиночная подпись с единицей («2,75 м»),
   * когда реальной трансформации нет и `outputs` пуст.
   */
  input: string | null;
  /** Распилы: «0,9×50», по возрастанию длины; пустой список — раскроя нет. */
  outputs: string[];
};
