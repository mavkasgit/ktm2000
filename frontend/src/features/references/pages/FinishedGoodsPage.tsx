// Вердикт по прототипу #143 (2026-09-06): вариант A — таблица (как у сырья) +
// карточка-модалка, блок операций — главный блок карточки.
// Прототип целиком (варианты A/B/C + switcher): ветка prototype/143-products-page.
// Реализация страницы — отдельная работа после спеки (#138).
export function FinishedGoodsPage() {
  return (
    <div className="py-12 text-center text-muted-foreground">
      <h2 className="text-xl font-semibold mb-2">Справочник готовой продукции</h2>
      <p>Страница в разработке</p>
    </div>
  );
}
